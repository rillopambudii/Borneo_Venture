; Raise upload limits so visitor/admin photos (often 2–12 MB) are accepted.
; Applies on PHP-FPM/CGI hosting (production). For `php artisan serve`,
; the limits come from the CLI php.ini instead.
upload_max_filesize = 20M
post_max_size = 25M
